
SPATH       := $($(LIBEVENT)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/libevent.mk rules/libevent.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(LIBEVENT)_CACHE_MODE  := GIT_CONTENT_SHA 
$(LIBEVENT)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(LIBEVENT)_DEP_FILES   := $(DEP_FILES)

